version: '3.8'

services:
  agentic-ai-platform:
    build: .
    ports:
      - "8003:8003"
    environment:
      - EXECUTION_MODE=mock
      - GCP_PROJECT_ID=gcp-10-project
    restart: unless-stopped
